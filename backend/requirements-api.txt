# Minimal dependencies to run the FastAPI API + job worker WITHOUT local
# GPU inference (LLM/image calls go through cloud providers instead).
# Use this set for cloud/free-tier deployment. For full local Ollama +
# Diffusers inference, use requirements-local-gpu.txt on top of this file.

fastapi==0.135.1
uvicorn==0.41.0
starlette==0.52.1
pydantic==2.12.5
pydantic_core==2.41.5
python-multipart==0.0.22
python-dotenv==1.2.2

SQLAlchemy==2.0.48
psycopg2-binary==2.9.11
alembic==1.14.0

httpx==0.28.1

pdfplumber==0.11.9
pdfminer.six==20251230
PyMuPDF==1.25.5
pytesseract==0.3.13
pillow==12.1.1

rapidfuzz==3.14.3

# Character NER. CHARACTER_EXTRACTION_MODE=llm (the hosted default) doesn't use
# this, but character_service.py still imports spacy at module load, so both are
# required. Drop them only once that import is made lazy.
spacy==3.8.11
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl#sha256=1932429db727d4bff3deed6b34cfc05df17794f4a52eeb26cf8928f7c1a0fb85

# Text-only cloud LLM SDKs (pick what your provider integration uses)
openai==2.26.0

# Cloudflare R2 storage (S3-compatible). Only needed if STORAGE_PROVIDER=r2.
boto3==1.35.99

# Auth + rate limiting
slowapi==0.1.9
